# Restaurant MVP App

This is a simple full-stack restaurant order app built with React and Express.

## Features
- View restaurant orders
- Frontend connects to backend API
- Runs with Docker

## How to Run
```bash
docker compose up --build